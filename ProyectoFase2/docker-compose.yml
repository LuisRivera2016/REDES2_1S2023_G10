version: '3.1'

services:
    backend:
        container_name: backend
        build:
            dockerfile: Dockerfile
            context: ./Backend
        ports:
            - 5000:5000

    frontend:
        container_name: frontend
        build:
            dockerfile: Dockerfile
            context: ./Frontend/cerberusPortal
        ports:
            - 80:80
